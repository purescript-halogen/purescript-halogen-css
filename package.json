{
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "clean": "rm -rf output .pulp-cache",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript": "0.11.4",
    "purescript-psa": "^0.5.0"
  }
}
