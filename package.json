{
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "clean": "rm -rf output .pulp-cache",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^12.2.0",
    "purescript": "0.12.0",
    "purescript-psa": "^0.6.0"
  }
}
