{
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "clean": "rm -rf output .pulp-cache",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript": "0.12.3",
    "purescript-psa": "^0.7.0"
  }
}
