{
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "clean": "rm -rf output .pulp-cache",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "purescript": "0.14.1",
    "purescript-psa": "^0.8.2"
  }
}
