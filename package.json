{
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "clean": "rm -rf output .pulp-cache",
    "build": "pulp build --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^9.0.1",
    "purescript": "0.10.1",
    "purescript-psa": "^0.3.9"
  }
}
